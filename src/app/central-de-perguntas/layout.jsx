import '../globals.css'

export const metadata = {
  title: 'Santa casa | Central de Perguntas',
  description: ''
}

export default function Layout({ children }) {
  return <main>{children}</main>
}
