import '../globals.css'

export const metadata = {
  title: 'Santa casa | Consultar agendamento',
  description: ''
}

export default function Layout({ children }) {
  return <main>{children}</main>
}
